streamlit>=1.33

# LangChain ecosystem - versions that work together
langchain>=0.1.14,<0.2.0
langchain-community==0.0.30
langsmith>=0.1.0,<0.2.0

# Parsing / IO / hub
pypdf>=3.0.0
beautifulsoup4>=4.12.2
requests>=2.31.0
tqdm>=4.66.1
huggingface-hub>=0.16.4
openai>=1.0.0

# Reliable vector store (avoid faiss binary problems)
chromadb>=0.3.30

# Optional: sentence-transformers (may pull torch; remove if it fails)
sentence-transformers>=2.2.2

# Keep faiss commented out — uncomment only if you control runtime or use Docker
# faiss-cpu==1.7.4
